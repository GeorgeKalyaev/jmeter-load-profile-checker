# Local tooling / secrets (never commit)
.filter-repo-replacements.txt

# Python
__pycache__/
*.py[cod]
*$py.class
.Python
.venv/
venv/
env/

# Bundled JMeter is not part of the repo — install separately
apache-jmeter-5.5/
